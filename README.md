# calendar

A small, carefully designed calendar that runs in the browser.

Plain HTML, CSS and JavaScript — no framework, no build step. Events are stored
in the browser, on your own machine.

See `cards/` for what this is, in plain language, and `plans/` for how it gets built.
